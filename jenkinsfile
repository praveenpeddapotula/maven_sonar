     pipeline {
        agent none
        stages {
         
          stage("build & sonarqube") {
            agent any
            steps {
              withSonarQubeEnv('sonar-scan') {
                sh 'mvn clean package sonar:sonar'
              }
            }
          }
        }
      }

